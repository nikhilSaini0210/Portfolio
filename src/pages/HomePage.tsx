import { type FC } from "react";

const HomePage: FC = () => {
  return (
    <main>
      <h1 className="text-4xl font-bold text-text-primary">Portfolio — Home</h1>
    </main>
  );
};

export default HomePage;
